<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TestSuitMedia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TestSuitMedia" parent="Base.Theme.TestSuitMedia" />
    <style name="TitleApp" parent="Base.Theme.TestSuitMedia">
        <item name="fontFamily">@font/poppins_semibold</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#04021D</item>
    </style>
    <style name="circularImageView" parent="Base.Theme.TestSuitMedia">
        <item name="cornerSize">50%</item>
    </style>
</resources>